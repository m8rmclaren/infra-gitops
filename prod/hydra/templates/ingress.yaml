{{- /*
Ingress for Hydra public endpoints
*/ -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hydra.fullname" . }}-public
  labels:
    {{- include "hydra.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  rules:
  - host: {{ .Values.gateway.hostname | quote }}
    http:
      paths:
      - path: /oauth2
        pathType: Prefix
        backend:
          service:
            name: {{ include "hydra.publicServiceName" . }}
            port:
              number: {{ .Values.hydra.service.public.port }}
      - path: /.well-known
        pathType: Prefix
        backend:
          service:
            name: {{ include "hydra.publicServiceName" . }}
            port:
              number: {{ .Values.hydra.service.public.port }}
      - path: /userinfo
        pathType: Prefix
        backend:
          service:
            name: {{ include "hydra.publicServiceName" . }}
            port:
              number: {{ .Values.hydra.service.public.port }}
      - path: /credentials
        pathType: Prefix
        backend:
          service:
            name: {{ include "hydra.publicServiceName" . }}
            port:
              number: {{ .Values.hydra.service.public.port }}

---
{{- /*
Ingress for Hydra admin endpoint with prefix-strip rewrite
(works with NGINX using regex + rewrite-target)
*/ -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hydra.fullname" . }}-admin
  labels:
    {{- include "hydra.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    {{- with .Values.ingress.adminAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  rules:
  - host: {{ .Values.gateway.hostname | quote }}
    http:
      paths:
      - path: /hydra-admin(/|$)(.*)
        # regex paths require ImplementationSpecific for NGINX
        pathType: ImplementationSpecific
        backend:
          service:
            name: {{ include "hydra.adminServiceName" . }}
            port:
              number: {{ .Values.hydra.service.admin.port }}

